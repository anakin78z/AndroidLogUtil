AndroidLogUtil
==============

A small utility to give a bit more control over logging in Android. 

uses:

log = new LogUtil("MainActivity");
log.d("this is a log!");

LogUtil.logW("this is a warning!");
